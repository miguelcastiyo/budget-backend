# Scenario definitions

`ScenarioCatalog.php` is the machine-readable catalog for the 24 Phase 0D groups. Each scenario declares a synthetic identity, fixed clock, authoritative PHP entry point, future consumer, and invariant coverage. Domain-specific steps should be added here only when the matching current-implementation adapter can execute them.
